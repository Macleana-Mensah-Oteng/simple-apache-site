name: Apache Deploy

on:
  push:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: apache-deploy
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Sync static site to remote Apache server
        uses: appleboy/scp-action@v0.1.4
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          source: "public-html/*"
          target: "/var/www/html/"
          rm: true

      - name: Restart Apache and validate deployment on remote server
        uses: appleboy/ssh-action@v0.1.4
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          script: |
            sudo systemctl restart apache2
            sudo systemctl is-active --quiet apache2
            curl --fail --retry 3 --retry-delay 2 --max-time 10 http://127.0.0.1/
            curl --fail --retry 3 --retry-delay 2 --max-time 10 http://127.0.0.1/about.html
